{% extends 'index.twig' %}

{% block breadcrumb_items %}
    <li><a href="{{ url('runs_list') }}">Runs</a></li>
    <li><a href="{{ url('runs_list', {source:source}) }}">{{ source }}</a></li>
    {% if symbol %}
        <li><a href="{{ url('single_run', {source:source,run:run}) }}">{{ run }}</a></li>
        <li>{{ symbol }}</li>
    {% else %}
        <li>{{ run }}</li>
    {% endif %}
        <li><a class="btn btn-default btn-xs" href="{{ url('single_callgraph', {source:source,run:run}) }}">Callgraph</a></li>
{% endblock %}

{% block body %}
    <div class="container">
        {{ block('breadcrumb') }}
    </div>
    <div class="container-fluid">
        {{ body|raw }}
    </div>
{% endblock %}